# Copy to repo root (or pass via --env):
#   cp examples/harmful-content.env.example .env
#
# Target = OpenRouter (model under test)
# Attacker + judge = LiteLLM proxy (deepseek/deepseek-chat)

OPENROUTER_API_KEY=
TARGET_API_KEY=

# LiteLLM master key for attacker/judge calls
LITELLM_API_KEY=
