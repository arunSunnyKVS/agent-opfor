{
  "name": "@astra/agent-mcp-server",
  "version": "0.1.0",
  "description": "Astra MCP server — expose red team tools to any MCP-compatible AI agent",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "astra-agent-mcp": "./dist/index.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "prepare": "echo 'run npm run build from the repo root'",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@astra/core": "*",
    "@modelcontextprotocol/sdk": "^1.10.2",
    "dotenv": "^17.4.2",
    "yaml": "^2.8.3",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.3"
  }
}
